import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.tomashobza',
  appName: 'itu-projekt',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
